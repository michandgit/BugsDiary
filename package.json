{
  "name": "bug-diary",
  "version": "1.0.0",
  "description": "A full-stack web application for tracking and managing coding bugs",
  "scripts": {
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "start": "concurrently \"cd backend && npm start\" \"cd frontend && npm run build && npm run preview\""
  },
  "keywords": ["bug-tracking", "react", "express", "fullstack"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}